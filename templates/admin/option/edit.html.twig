{% extends "base.html.twig" %}

{% block title 'Editer les biens' %}

{% block body %}

<div class="container">
    <h1>Editer l'option </h1>
    {{ form_start(form) }}

    <div class="row">
    <div class="col-md-4">
        {{ form_row(form.name) }}
    </div>


    </div>

    {{ form_rest(form) }}
    <button class="btn btn-primary">
        Editer
    </button>
    {{ form_end(form) }}

</div>









{% endblock %}